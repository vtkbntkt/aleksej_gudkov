<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:checkableBehavior="single"
        android:id="@+id/task_group">
        <item
            android:id="@+id/all_ref"
            android:icon="@drawable/ic_all_refs"
            android:title="@string/all_refs"/>
        <item
            android:id="@+id/map_ref"
            android:icon="@drawable/ic_map_refs"
            android:title="@string/map_refs" />
    </group>

    <group
        android:id="@+id/account_group"
        android:checkableBehavior="none">
        <item
            android:id="@+id/login_item"
            android:title="@string/login"/>
    </group>


</menu>
